# ──────────────────────────────────────────────────────────
# Core Snowflake connectivity
snowflake-connector-python>=3.6.0          # DB driver
snowflake-snowpark-python>=1.12.0          # Optional Snowpark API

# Data-handling & schema inference
pandas>=2.2.0
numpy>=1.24.0
pyarrow>=15.0.0                            # High-speed parquet/CSV I/O
scikit-learn>=1.4.0                        # ML helpers for type detection
python-dateutil>=2.9.0                     # Robust datetime parsing

# Cloud access
azure-storage-blob>=12.20.0                # Connect to Azure containers
azure-identity>=1.15.0                     # Managed-identity / SAS auth

# Config & env
python-dotenv>=1.0.0
PyYAML>=6.0

# Orchestration, logging, UX
tabulate>=0.9.0                            # Pretty table previews
tqdm>=4.66.4                               # Progress bars
rich>=13.7.0                               # Colourful structured logs
loguru>=0.7.2                              # Friendly logging wrapper

# ──────────────────────────────────────────────────────────
# MULTI-AGENT SYSTEM DEPENDENCIES
# ──────────────────────────────────────────────────────────

# Google GenAI SDK (official replacement for deprecated google-generativeai)
google-genai>=1.16.0                       # Unified Google GenAI SDK for Gemini 2.0+ models

# LangChain ecosystem for multi-agent coordination
langchain>=0.3.25                          # Core LangChain framework
langchain-google-genai>=2.1.5              # Google GenAI integration for LangChain
langchain-community>=0.3.24                # Community extensions
langchain-text-splitters>=0.3.8            # Text processing utilities

# Embeddings and vectorization
fastembed>=0.7.0                           # Fast embedding models
onnxruntime>=1.17.0                        # ONNX runtime for ML models

# Additional AI/ML dependencies
transformers>=4.0.0                        # Hugging Face transformers (when added)
instructor>=1.0.0                          # Structured outputs (when added)

# Schema versioning (if you still want Alembic for optional transforms)
SQLAlchemy>=2.0.30
alembic>=1.13.1

# Testing
pytest>=8.2.0
pytest-mock>=3.12.0
coverage>=7.5.0

# Note: Model Context Protocol (MCP) dependencies will be added when
# upgrading to Python 3.10+ as MCP requires newer Python versions
